dependencies:
    override:
        - make installDependencies
test:
    override:
        - make test
    post:
        - make install
        - make installARM
        - sudo cp * $CIRCLE_ARTIFACTS:
            pwd:
                bin

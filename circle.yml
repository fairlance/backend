general:
    build_dir: /home/ubuntu/src/github.com/fairlance
dependencies:
        - make installDependencies
test:
    override:
        - make test
    post:
        - make install
        - make installARM
        - sudo cp * $CIRCLE_ARTIFACTS:
            pwd: bin

dependencies:
    override:
        - make installDependencies
test:
    override:
        - make testShort
    post:
        - make install
        - make installARM
        - sudo cp * $CIRCLE_ARTIFACTS:
            pwd:
                bin
